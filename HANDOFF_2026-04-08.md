## Fake News Detector - Handoff (2026-04-08)

### Что сделали сегодня
- Усилили `src/factcheck/retrieval.py`:
  - добавили stopword-aware overlap;
  - добавили fast-query selector с support-friendly доменами;
  - подключили site adapters для `apnews.com` и `bbc.com`;
  - добавили DDG fallback для части `site:` queries;
  - добавили фильтры для listing pages, low-match fact-check pages и low-match generic news pages;
  - обновили search cache key до `search_v6`.
- Усилили `src/factcheck/evidence.py`:
  - добавили инверсию `explicit_verdict`, если fact-check page проверяет противоположно сформулированный claim;
  - добавили guard для generic news: title должен подтверждать stance, иначе evidence понижается до `neutral`.
- Усилили `src/factcheck/decision.py`:
  - `claim prior` больше не проталкивает hard verdict, если retrieval дал только `neutral` evidence.
- Усилили `src/factcheck/claims.py`:
  - добавили merged candidate из короткого предложения + следующего, чтобы retrieval видел больше контекста.
- Добавили тесты в `tests/test_factcheck_core.py`:
  - support-friendly fast queries;
  - explicit verdict inversion;
  - neutral-only prior guard;
  - generic-news title alignment;
  - merged claim extraction.

### Что проверили
- `python -m unittest discover -s tests -v` - green (`17` tests).
- Внутренний benchmark:
  - `outputs/factcheck_runs/v3_after_support_round.json`
  - `accuracy=1.0`
  - `uncertain_rate=0.2333`
  - `error_rate=0.0`

### Что сработало
- Исправился ряд явных ошибок на live кейсах:
  - `Matt Gaetz ... hospice fraud` снова даёт `fake` через `explicit_factcheck_override`.
  - Появились релевантные BBC/AP retrieval hits для support-like claims.
  - Снизился шум от archive/search/listing pages.

### Что не сработало / где остановились
- Полный `AVeriTeC dev` после support-wave всё ещё регрессирует против `round3`:
  - старый лучший файл: `outputs/factcheck_runs/averitec_dev_eval_round3.json`
  - `accuracy=0.328`, `uncertain_rate=0.620`
  - новый полный прогон: `outputs/factcheck_runs/averitec_dev_eval_support_round_full.json`
  - `accuracy=0.208`, `uncertain_rate=0.798`
- Главная причина: generic news retrieval всё ещё иногда создаёт ложный counter-evidence и/или уводит в `uncertain`.
- Неприятный кейс, который ещё не добит:
  - `Wearing face masks will stop the spread of covid 19`
  - `politifact.com` даёт корректный `support`, но рядом иногда приходит шумный `bbc.co.uk` doc и блокирует hard verdict.

### Следующий шаг на завтра
1. Дочистить entity extraction в `src/factcheck/claims.py`:
   - убрать ложные single-token entities типа `Wearing`, `Speaking`, `More`, если это sentence-initial generic word.
2. Снова прогнать точечные кейсы:
   - masks
   - Amy Coney Barrett confirmation
   - Matt Gaetz hospice fraud
3. После этого повторить:
   - `AVeriTeC` sample 100
   - если sample улучшился, полный `AVeriTeC dev`
4. Только потом трогать decision policy.

### Важные файлы
- `src/factcheck/retrieval.py`
- `src/factcheck/evidence.py`
- `src/factcheck/decision.py`
- `src/factcheck/claims.py`
- `tests/test_factcheck_core.py`
- `outputs/factcheck_runs/v3_after_support_round.json`
- `outputs/factcheck_runs/averitec_dev_eval_round3.json`
- `outputs/factcheck_runs/averitec_dev_eval_support_round_full.json`
