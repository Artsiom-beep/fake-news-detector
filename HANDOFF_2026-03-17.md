# HANDOFF — fake-news-detector (2026-03-17)

## 1) Что произошло
- Были частые таймауты/падения из-за rate-limit/cooldown провайдера модели + длинных прогонов.
- Исправлен retrieval-баг с Google wrappers (`consent.google.com/news.google.com`).
- Стабилизирован режим тестов: лучше запускать по 1 URL за процесс.

## 2) Текущее состояние продукта
- Пайплайн стабилен в «бережном» режиме (single-link tests).
- На тестовом фейк-наборе результат устойчивый: **2/4 true, 2/4 uncertain**.
- Узкое место: extraction/NLI для Reuters и Boomlive fact-check страниц.

## 3) Важные коммиты (последние ключевые)
- `1f9801f` fix retrieval: filter google consent/news wrappers and prefer source URLs
- `5710643` stability: default UI to fast mode and reduce evidence fanout
- `497043d` chore: ignore detector artifacts and add stable runbook/scripts
- `24eab38` align LIAR config and add text-only dataset/model options
- `72db9c2` add domain-specific anchor claim extraction for fact-check pages
- `4113bbf`, `db4315c`, `a2a9ca7` fact-check extraction tuning

## 4) Как запускать стабильно
```powershell
cd C:\Users\OpenClaw\.openclaw\workspace\projects\fake-news-detector
.\.venv\Scripts\Activate.ps1
python -m uvicorn src.ui:app --host 0.0.0.0 --port 8081
```

Тесты:
- Только **1 ссылка за запуск**
- Пауза 20–30 сек между запусками
- `set_fast_mode(True)` для устойчивости

## 5) Проверенные результаты (фейк-ссылки)
- Reuters fact-check: uncertain (0.35)
- AFP fact-check: true (0.5)
- Newschecker fact-check: true (0.5)
- Boomlive fact-check: uncertain (0.125)

## 6) Что делать дальше (приоритет)
1. Сделать `claim rerank` по NLI-score (не только regex anchor)
2. Добавить domain parser для Reuters/Boomlive: извлекать блоки "Claim / Fact"
3. Довести цель до 3/4+ на текущем тестовом наборе
4. Сохранить benchmark script и автоматический отчёт

## 7) Repo hygiene
- Для `projects/fake-news-detector` статус очищен по артефактам через `.gitignore`.
- В корне workspace остаются сторонние/исторические изменения вне FND (не трогать без отдельного решения).
